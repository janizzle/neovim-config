-- Vim motion practice game: `:VimBeGood` opens a scratch buffer with drills
-- (relative jumps, dd/ci, hjkl, ...). Loaded only when the command is run.

return {
  "ThePrimeagen/vim-be-good",
  cmd = "VimBeGood",
}
